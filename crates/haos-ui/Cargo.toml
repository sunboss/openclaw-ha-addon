[package]
name = "haos-ui"
version = "0.1.0"
edition = "2024"

[dependencies]
axum.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["io-util", "macros", "net", "rt-multi-thread", "sync", "time"] }
